export default class User {
  constructor(username, email, password, fullname, phone, address) {
    this.username = username;
    this.email = email;
    this.password = password;
    this.fullname = fullname;
    this.phone = phone;
    this.address = address;
  }
}
